services:
  - type: web
    name: threadlabs
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm run start:server
    envVars:
      - key: OPENAI_API_KEY
        sync: false